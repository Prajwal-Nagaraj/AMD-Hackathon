[project]
name = "track1-agent"
version = "0.1.0"
description = "Token-efficient general-purpose agent for AMD Hackathon Track 1"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.50.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
